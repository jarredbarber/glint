# Glint

Read `CLAUDE.md` for the current contributor guide. It is the source of truth for commands, architecture, SPA routes, and change rules.

Glint is a static Markdown wiki and self-contained HTML renderer. It has no Glint server, backend credential store, or live-reload service. Browser edits go directly through the active local, Drive, or GitHub storage adapter.
